using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace PShochu.PInvoke
{
    public class Constants
    {
        static readonly public IntPtr NULL = (IntPtr)0;
        static readonly public IntPtr INVALID_HANDLE_VALUE = new IntPtr(-1);
    }
}
